<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %> 
<body>
    <%- include('partials/header') %> 
    <main>
        <h1>Editar la película...</h1>
        <section class="movieForm">
            <form action="?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="title">Título</label>
                    <input type="text" name="title" id="title" value="<%= pelicula.title %>">
                </div>
                <div class="form-group">
                    <label for="rating">Rating</label>
                    <input type="number" name="rating" id="rating" value="<%= pelicula.rating %>">
                </div>
                <div class="form-group">
                    <label for="awards">Premios</label>
                    <input type="number" name="awards" id="awards" value="<%= pelicula.awards %>">
                </div>
                <div class="form-group">
                    <label for="awards">Paises</label>
                    <select name="" id="">
                        <% for( let i = 0; i < paises.length; i++ ) { %>
                            <option><%= paises[i].name %></option>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label for="">Duración (en minutos)</label>
                    <input type="number" name="length" id="length" value="<%= pelicula.length %>">
                </div>
                <button type="submit">Actualizar</button>
            </form>
        </section>
    </main>
    <%- include('partials/footer') %> 
</body>
</html>